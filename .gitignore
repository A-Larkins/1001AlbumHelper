bin/
obj/
dist/
output/
appsettings.json
*.xlsx

# Google OAuth secrets / cached token
credentials.json
client_secret*.json
.google-sheets-token/

# Google service-account key (potentials sync) — secret, never commit
service-account.json
service-account*.json

# macOS finder metadata
.DS_Store

# Xcode per-user state (from the SignHelper signing stub)
xcuserdata/
*.xcuserstate
